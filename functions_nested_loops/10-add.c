#include "main.h"

/**
 *print_last_digit  - print las digit of any number
 *
 * @a: the firs int to add
 * @b: sec int to add
 * Return: res
 */
int add(int a, int b)
{
    int res = a + b;
    _putchar('0' + res);
    _putchar('\n');
    return(res);
}
