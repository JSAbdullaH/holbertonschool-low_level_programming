#include "main.h"

/**
 * _islower  - prints the alphabet 10 times
 *
 * Return: 1 if c lower case , 0 othgrwise
 */
int _islower(int c)
{
    if(c >= 'a' && c <= 'z')
    return(1);
    else
    return(0);
}
