#include "main.h"

/**
 *_isupper - get char and check its upper or not
 *
 *Return: 1 if its upper , 0 otherwise
 */
int _isupper(int c)
{
    if(c >= 'A' && c <= 'Z')
    return (1);
    else
    return (0);

}
