#include "main.h"

/**
 *_isdigit - get char and check its digit or not
 *
 *Return: 1 if its digit , 0 otherwise
 */
int _isdigit(int c)
{
	int c;
    if(c >= '0' && c <= '9')
    return (1);
    else
    return (0);

}
